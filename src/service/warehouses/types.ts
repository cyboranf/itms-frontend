export type Warehouse = {
  id: number;
  building: string;
  zone: string;
  spaceId: number;
  spaceHeight: number;
  spaceWidth: number;
  spaceLength: number;
  productId: number;
  productName: string;
  productCode: string;
}

//TODO czmemu warehouse ma id, name, code produktu?
